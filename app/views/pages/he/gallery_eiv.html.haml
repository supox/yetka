= javascript_pack_tag 'gallery'
- images = Dir.glob("app/assets/images/eiv_suite/*.jpg").map{|path| path.split("/").last }

%h1 בקתת איב

%div.main-img
    =image_tag "eiv_suite/#{images[0]}", id: "current"

%div.imgs
    -images.each do |i|
        =image_tag "eiv_suite/#{i}"
